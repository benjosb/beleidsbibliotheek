# Andere gemeenten

Bij een **nieuwe** gemeente: maak hier een map met een korte slug, bijv. `rotterdam/` of `den-haag/`.

Houd het plat: liever `gemeenten/<slug>/offerte.md` dan vier submappen die je zelden opent.
